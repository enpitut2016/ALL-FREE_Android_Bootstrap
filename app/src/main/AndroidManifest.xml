<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.allfree.conotan_bootstrap">

    <application
        android:name=".Bootstrap"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SendActivity"
            android:label="@string/title_activity_send"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FinishedSendingActivity"
            android:label="@string/title_activity_finished_sending"/>
	<activity
            android:name=".HelpDetailActivity"
            android:label="@string/title_activity_help_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RememberActivity"></activity><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>
